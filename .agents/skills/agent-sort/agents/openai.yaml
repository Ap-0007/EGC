interface:
  display_name: "Agent Sort"
  short_description: "Evidence-backed EGC install planning"
  brand_color: "#0EA5E9"
  default_prompt: "Use $agent-sort to build an evidence-backed EGC install plan."
policy:
  allow_implicit_invocation: true
